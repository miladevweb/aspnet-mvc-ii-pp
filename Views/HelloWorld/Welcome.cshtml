@* We're giving values of a variable from a Controller to View *@
<h5>Your Number of Times is @ViewData["NumTimes"]</h5>

<ul>
    @for(int i=0 ;  i < (int)ViewData["NumTimes"]!; i++)
    {
        <li>@ViewData["Message"]</li>
    }
</ul>